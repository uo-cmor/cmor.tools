# This file defines the targets in the project analysis plan

# Simple skeleton outline of analysis plan - revise as required
targets <- list(
#   tar_target(data_file, "raw_data/data.csv", format = "file"),
# 	tar_target(data, read_raw_data(data_file)),
# 	tar_target(clean, process_data(data)),
# 	tar_target(results, analyse_data(clean)),
#   tar_target(file_word_styles, "reports/word-styles-reference-01.docx",
#              format = "file"),
#   tar_target(file_csl, "reports/vancouver.csl", format = "file"),
#   tar_target(file_bib, "reports/references.bib", format = "file"),
# 	tar_render(
#     manuscript,
#     "reports/manuscript.Rmd",
#     output_format = rmarkdown::word_document(
#			  reference_docx = file_word_styles,
#			  pandoc_args = list("--csl", file_csl, "--bibliography", file_bib)
#		  ),
#		  output_file = "output/manuscript.docx"
#   )
)
